apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service-green
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-service-green
  template:
    metadata:
      labels:
        app: appointment-service-green
    spec:
      containers:
      - name: appointment-service
        image: 225989377732.dkr.ecr.us-east-1.amazonaws.com/medi_track_appointment:green
        ports:
        - containerPort: 5000
        env:
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: db-credentials
              key: password
