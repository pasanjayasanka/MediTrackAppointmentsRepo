apiVersion: v1
kind: Service
metadata:
  name: appointment-service-green
spec:
  selector:
    app: appointment-service-green
  type: LoadBalancer
  ports:
   - protocol: TCP
     port: 80
     targetPort: 5000
