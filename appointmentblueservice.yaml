apiVersion: v1
kind: Service
metadata:
  name: appointment-service-blue
spec:
  selector:
    app: appointment-service-blue
  type: LoadBalancer
  ports:
   - protocol: TCP
     port: 80
     targetPort: 5000
